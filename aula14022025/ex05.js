// Laços de repetição

// forEach

const numeros = [1, 2, 3, 4, 5]

// numeros.forEach(n => console.log("Número: " + n))

// map

const dobrados = numeros.map(n => n * 2)
dobrados.forEach(n => console.log(n))